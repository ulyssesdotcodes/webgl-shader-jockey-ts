class VideoDistortionRenderer extends ObjectRenderer {
  constructor(plane: ObjectMaterial) {
    super(plane);
  }

  update(updateData: any, resolution: THREE.Vector2): void {
    super.update(updateData, resolution);
  }
}
